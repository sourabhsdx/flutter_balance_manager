import 'package:flutter/material.dart';

final kInputBox = InputDecoration(
  labelText: "Initial Amount",
  border: OutlineInputBorder(
    borderRadius: BorderRadius.circular(8),
  ),
  disabledBorder: OutlineInputBorder(
      borderRadius: BorderRadius.circular(8)),
);